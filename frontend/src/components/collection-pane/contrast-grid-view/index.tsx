import { Section } from "../../layout";
import { ContrastGridContent } from "./grid";

export const ContrastGrid = () => {
  return (
    <Section title="Contrast Grid">
      <ContrastGridContent />
    </Section>
  );
}